trigger:
  branches:
    include:
      - main
      - features

pool: Agents_pool

stages:
  - stage: terraforminstall
    displayName: terraform install
    jobs:
      - job: terraforminstall
        displayName: terraform install
        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'

  - stage: terraforminitvalidateplan
    displayName: terraform init validate plan
    jobs:
      - job: terraforminitvalidateplan
        displayName: terraform init validate plan
        steps:
        - task: TerraformTask@5
          displayName: terraform init
          inputs:
            provider: 'azurerm'
            command: 'init'
            workingDirectory: '$(System.DefaultWorkingDirectory)/environment'
            backendServiceArm: 'connection_ado'
            backendAzureRmStorageAccountName: 'yuvrajstg'
            backendAzureRmContainerName: 'yuvrajcontainer'
            backendAzureRmKey: 'backen.tfstate'

        - task: TerraformTask@5
          displayName: terraform validate
          inputs:
            provider: 'azurerm'
            command: 'validate'
            workingDirectory: '$(System.DefaultWorkingDirectory)/environment'

        - task: TerraformTask@5
          displayName: terraform plan
          inputs:
            provider: 'azurerm'
            command: 'plan'
            environmentServiceNameAzureRM: 'connection_ado'
            workingDirectory: '$(System.DefaultWorkingDirectory)/environment'
